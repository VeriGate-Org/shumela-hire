# Test profile configuration (Spring Boot 3.x)
# Note: spring.profiles.active cannot be set in profile-specific files
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# Disable Flyway for H2 tests
spring.flyway.enabled=false

# Security - test-only keys (NOT for production use)
jwt.secret=dGVzdC1vbmx5LWp3dC1zZWNyZXQta2V5LXRoYXQtaXMtYXQtbGVhc3Qtc2l4dHktZm91ci1ieXRlcy1sb25nLWZvci1oczUxMi1hbGdvcml0aG0=
encryption.key=dGVzdC1vbmx5LWVuY3J5cHRpb24ta2V5LWZvci10ZXN0cw==

# Cache configuration for testing
spring.cache.type=simple

# Disable Redis for tests
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration,org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration

# Disable SQS for tests
notification.sqs.enabled=false

# Logging configuration
logging.level.com.arthmatic.shumelahire=DEBUG
